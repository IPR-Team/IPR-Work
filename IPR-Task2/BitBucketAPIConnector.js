// Implementation not possible!
//The provided API allows only a filtered search, within teams or if a username
//is known. Otherwise you get all projects back. Going through this would increase
//the waiting time.

function BitBucketAPIConnector(){
  this.searchForProjects = function(search_string, amount_of_results, page, createdBeforeDate, callback){
  }
}
